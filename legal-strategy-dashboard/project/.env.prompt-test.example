# Copy this file to `.env.prompt-test` and populate values.
# Used by: scripts/chatgpt/test_prompt_id_responses.sh

# Required
OPENAI_API_KEY=your_openai_api_key_here

# Optional headers
OPENAI_ORG=
OPENAI_PROJECT=

# Required key permissions (configured in OpenAI dashboard on the key itself):
# - api.responses.write
# Recommended:
# - api.model.read

